#pragma once

// Generated by CMake from cmake/coro_version.h.in — see README.md's
// "Embedding the version in the built binary". @CORO_VERSION@ comes from
// the Conan recipe's set_version() (conanfile.py); "unknown" when
// configuring without Conan, since CORO_VERSION is never set in that case.
#define CORO_VERSION "@CORO_VERSION@"
